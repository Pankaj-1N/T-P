<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tp">

    <!-- ✅ Needed for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="T&amp;P App"
        android:theme="@style/Theme.TP">

        <!-- Launcher Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboards -->
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".StudentDashboardActivity" />

        <!-- Admin feature screens -->
        <activity android:name=".ManageNoticesActivity" />
        <activity android:name=".NoticesActivity" />

        <activity android:name=".ManagePlacementsActivity" />
        <activity android:name=".PlacementRecordsActivity" />

        <activity android:name=".ManageQAActivity" />
        <activity android:name=".QAManagementActivity" />

        <activity android:name=".ManageStudentsActivity" />
        <activity android:name=".StudentsActivity" />

        <!-- Others -->
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ProfileActivity" />

    </application>
</manifest>
