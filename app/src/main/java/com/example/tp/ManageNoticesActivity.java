package com.example.tp;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class ManageNoticesActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Using your existing placeholder layout to avoid resource errors for now
        setContentView(R.layout.simple_placeholder);
        setTitle("Manage Notices");
        // TODO: Replace simple_placeholder with a real layout when ready (e.g., activity_manage_notices)
    }
}
