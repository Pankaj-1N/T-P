package com.example.tp;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class ManageQAActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Placeholder layout until you create a proper one
        setContentView(R.layout.simple_placeholder);
        setTitle("Manage Q&A");
        // TODO: Replace simple_placeholder with a proper layout (e.g., activity_manage_qa)
    }
}
