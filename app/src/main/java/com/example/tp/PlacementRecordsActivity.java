package com.example.tp;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class PlacementRecordsActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Temporary layout until your actual design is ready
        setContentView(R.layout.simple_placeholder);
        setTitle("Placement Records");
        // TODO: Replace simple_placeholder with the real layout (e.g., activity_placement_records)
    }
}
